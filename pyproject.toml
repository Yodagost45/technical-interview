[project]
name = "technical-interview"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Awen Saunders", email = "me@awen.io" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128.0",
]

[project.scripts]
technical-interview = "technical_interview:main"

[build-system]
requires = ["uv_build>=0.9.27,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "fastapi[standard]>=0.128.0",
]
